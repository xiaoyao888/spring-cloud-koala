spring:
  cloud:
    config:
      uri: http://yangwensheng:123456@spring-cloud-discovery:8080/
      label: master
      profile: ${spring.profiles.active}
      ignoredInterfaces:
        - docker0
        - veth.*
        - VM.*